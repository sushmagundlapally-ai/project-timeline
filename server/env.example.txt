# Project Timeline - Environment Configuration
# Rename this file to .env and update values

# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration
# For local MongoDB:
MONGODB_URI=mongodb://localhost:27017/project-timeline

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/project-timeline?retryWrites=true&w=majority

# For enterprise MongoDB:
# MONGODB_URI=mongodb://<username>:<password>@<host>:<port>/project-timeline?authSource=admin

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Optional: Authentication (for future use)
# JWT_SECRET=your-super-secret-key-change-in-production
# JWT_EXPIRES_IN=7d

